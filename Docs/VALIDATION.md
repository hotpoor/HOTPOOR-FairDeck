# v0.1 验证记录

2026-09-21，本地 Windows 环境。

## 已通过

- Unity 6000.6.2f1 首次编译、生成 WalletDesk 场景与构建场景配置，batchmode 返回 0。
- Unity 钱包生成、写入/读取、签名验证；错误密码和密文篡改被拒绝。
- Python 服务端验证 Unity 生成的公开签名样本，地址计算一致。
- 隔离 PostgreSQL 实例中的 8 项测试：分片校验、Unity 签名、跨端口认证、无效签名、并发挑战重放、节点成员权限、归档权限及故障/篡改处理、不可变写入冲突。
- 发布前检查暂存文件：没有本机已知凭据值，没有 wallet、.secrets、数据库或 Unity 缓存进入提交。

## 尚未验证或实现

- 目标服务器 DNS、TLS、OpenResty 实际加载、多进程压测。
- 七牛真实桶上传/回读；当前归档集成测试使用本地对象存储替身。
- Unity 真机图形交互验收和 Windows 独立构建。
- 完整游戏、共识、P2P 同步、零知识洗牌、终局恢复与四档解密协议。

公开仓库中的配置和示例不包含实际凭据；本记录不代表生产就绪或密码学安全审计通过。

## 鼠标手部交互增量验证

2026-09-21：Windows 开发构建成功。程序内冒烟测试验证开场面板关闭、拿起后打开、放回后关闭，记录 FAIRDECK_HAND_SMOKE_OK；截图检查并修正卡片文字尺寸。使用实际窗口鼠标点击卡片与 Esc 放回复测通过。未创建真实钱包或提交真实账号信息。

手部目前为可动几何体原型，尚未替换正式美术模型。Windows 试玩程序位于 Unity 项目的 Builds/Windows/，不提交构建产物或 wallet 文件。
